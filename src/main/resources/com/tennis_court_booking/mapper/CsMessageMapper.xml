<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tennis_court_booking.cs.mapper.CsMessageMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cs_message (conversation_id, sender_type, sender_user_id, content, created_at)
        VALUES (#{conversationId}, #{senderType}, #{senderUserId}, #{content}, #{createdAt})
    </insert>

    <select id="listByConversationIdDesc" resultType="CsMessage">
        SELECT id, conversation_id AS conversationId, sender_type AS senderType,
               sender_user_id AS senderUserId, content, created_at AS createdAt
        FROM cs_message
        WHERE conversation_id = #{conversationId}
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>
</mapper>
